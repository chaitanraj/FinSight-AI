# Web framework
flask
gunicorn
flask-cors
python-dotenv

# LLM provider
groq

# Vector store (ONNX embeddings — no PyTorch needed)
chromadb
onnxruntime
tokenizers

# PDF ingestion (only needed locally for re-ingesting docs)
# pip install pypdf langchain langchain-community langchain-text-splitters
